catalogs:
  iceberg_minio: |
    connector.name=hive
    iceberg.catalog.type=hive_metastore
    iceberg.security=system
    hive.metastore.uri=thrift://hive.spark.svc.cluster.local:9083
    hive.s3.aws-access-key=UjyRAOqhpQXnW2xBbWxj
    hive.s3.aws-secret-key=Y1M1kyok0rkxWIfPMZYckxkMzZ9D8YEQa9Y6Dk27
    hive.s3.endpoint=https://minios3.campus.clusterdiali.me
    hive.s3.path-style-access=true
    hive.s3.ssl.enabled=true
    hive.metastore.thrift.client.connect-timeout=20s
    hive.metastore.thrift.client.read-timeout=20s
    hive.metastore.thrift.client.ssl.enabled=true
    hive.metastore.thrift.client.ssl.trust-certificate=/etc/starburst/truststore/minio.crt
    hive.metastore.thrift.client.ssl.key=/etc/starburst/truststore/minio.key

volumeMounts:
  - name: starburst-truststore-hms
    mountPath: /etc/starburst/truststore
    readOnly: true
volumes:
  - name: starburst-truststore-hms
    secret:
      secretName: starburst-truststore-hms
